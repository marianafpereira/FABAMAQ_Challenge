// SearchBar.js
import React from 'react';

const SearchBar = ({ onSearchChange }) => {
    // return (
    //     <input type="text" placeholder="Search users..." onChange={onSearchChange} />
    // );
};

export default SearchBar;